Scripts to make:
1. Project setup scripts Auto-create folders, virtual environments, Git repos, README, etc. 
2. Auto: git add, commit, push in one command
3. Scrape websites for:
	Notes
	Job postings
	News
4. A quick projec/config file/bookmark (file or browser), winodw setup (splits, worksapce) setup. Triggered by shortuct, presents a window to search for these by names.
5. A sanatizer remove $ or 1 from copied commands.
6. A mail sender that sends copied text, file via gmail to specified mails. Subjet heading the file name. Can attach files (if path is copied), or text. Sends notifiaction when compleetion. It is explitly launched by user, and on lauching it goes and sends the lasst copied item and sends it (if path then uploads file).