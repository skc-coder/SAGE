**Method**: Spiral rule (Right-Left rule).
1. Find the **identifier** (start point).
2. Look **right** first, till end or `)`, then **left**.
3. Translate symbols:
   - `*` → "pointer to"
   - `[]` → "array of"
   - `()` → "function returning"
## [[Pointer to Function]]
